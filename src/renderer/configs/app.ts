import ConfigLoadServiceProvider from "../serviceProviders/ConfigLoadServiceProvider";
import AppServiceProvider from "../serviceProviders/AppServiceProvider";


export const serviceProviderClasses = [
    AppServiceProvider,
    ConfigLoadServiceProvider,
];

